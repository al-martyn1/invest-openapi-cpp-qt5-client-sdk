/**
 * OpenAPI
 * tinkoff.ru/invest OpenAPI.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: n.v.melnikov@tinkoff.ru
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "MarketApi.h"
#include "Helpers.h"

#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

MarketApi::MarketApi(const QString &scheme, const QString &host, int port, const QString &basePath, const int timeOut)
    : _scheme(scheme),
      _host(host),
      _port(port),
      _basePath(basePath),
      _timeOut(timeOut),
      isResponseCompressionEnabled(false),
      isRequestCompressionEnabled(false) {}

MarketApi::~MarketApi() {
}

void MarketApi::setScheme(const QString &scheme) {
    _scheme = scheme;
}

void MarketApi::setHost(const QString &host) {
    _host = host;
}

void MarketApi::setPort(int port) {
    _port = port;
}

void MarketApi::setBasePath(const QString &basePath) {
    _basePath = basePath;
}

void MarketApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void MarketApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void MarketApi::addHeaders(const QString &key, const QString &value) {
    defaultHeaders.insert(key, value);
}

void MarketApi::enableRequestCompression() {
    isRequestCompressionEnabled = true;
}

void MarketApi::enableResponseCompression() {
    isResponseCompressionEnabled = true;
}

void MarketApi::abortRequests(){
    emit abortRequestsSignal();
}

void MarketApi::marketBondsGet() {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/bonds");

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketBondsGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketBondsGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    MarketInstrumentListResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketBondsGetSignal(output);
        emit marketBondsGetSignalFull(worker, output);
    } else {
        emit marketBondsGetSignalE(output, error_type, error_str);
        emit marketBondsGetSignalEFull(worker, error_type, error_str);
    }
}

void MarketApi::marketCandlesGet(const QString &figi, const QDateTime &from, const QDateTime &to, const CandleResolution &interval) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/candles");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("figi")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(figi)));

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("from")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(from)));

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("to")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(to)));

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("interval")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(interval)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketCandlesGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketCandlesGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    CandlesResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketCandlesGetSignal(output);
        emit marketCandlesGetSignalFull(worker, output);
    } else {
        emit marketCandlesGetSignalE(output, error_type, error_str);
        emit marketCandlesGetSignalEFull(worker, error_type, error_str);
    }
}

void MarketApi::marketCurrenciesGet() {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/currencies");

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketCurrenciesGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketCurrenciesGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    MarketInstrumentListResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketCurrenciesGetSignal(output);
        emit marketCurrenciesGetSignalFull(worker, output);
    } else {
        emit marketCurrenciesGetSignalE(output, error_type, error_str);
        emit marketCurrenciesGetSignalEFull(worker, error_type, error_str);
    }
}

void MarketApi::marketEtfsGet() {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/etfs");

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketEtfsGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketEtfsGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    MarketInstrumentListResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketEtfsGetSignal(output);
        emit marketEtfsGetSignalFull(worker, output);
    } else {
        emit marketEtfsGetSignalE(output, error_type, error_str);
        emit marketEtfsGetSignalEFull(worker, error_type, error_str);
    }
}

void MarketApi::marketOrderbookGet(const QString &figi, const qint32 &depth) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/orderbook");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("figi")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(figi)));

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("depth")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(depth)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketOrderbookGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketOrderbookGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    OrderbookResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketOrderbookGetSignal(output);
        emit marketOrderbookGetSignalFull(worker, output);
    } else {
        emit marketOrderbookGetSignalE(output, error_type, error_str);
        emit marketOrderbookGetSignalEFull(worker, error_type, error_str);
    }
}

void MarketApi::marketSearchByFigiGet(const QString &figi) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/search/by-figi");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("figi")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(figi)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketSearchByFigiGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketSearchByFigiGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    SearchMarketInstrumentResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketSearchByFigiGetSignal(output);
        emit marketSearchByFigiGetSignalFull(worker, output);
    } else {
        emit marketSearchByFigiGetSignalE(output, error_type, error_str);
        emit marketSearchByFigiGetSignalEFull(worker, error_type, error_str);
    }
}

void MarketApi::marketSearchByTickerGet(const QString &ticker) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/search/by-ticker");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("ticker")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(ticker)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketSearchByTickerGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketSearchByTickerGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    MarketInstrumentListResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketSearchByTickerGetSignal(output);
        emit marketSearchByTickerGetSignalFull(worker, output);
    } else {
        emit marketSearchByTickerGetSignalE(output, error_type, error_str);
        emit marketSearchByTickerGetSignalEFull(worker, error_type, error_str);
    }
}

void MarketApi::marketStocksGet() {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/market/stocks");

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &MarketApi::marketStocksGetCallback);
    connect(this, &MarketApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void MarketApi::marketStocksGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    MarketInstrumentListResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit marketStocksGetSignal(output);
        emit marketStocksGetSignalFull(worker, output);
    } else {
        emit marketStocksGetSignalE(output, error_type, error_str);
        emit marketStocksGetSignalEFull(worker, error_type, error_str);
    }
}

} // namespace OpenAPI
