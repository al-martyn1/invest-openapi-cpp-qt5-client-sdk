/**
 * OpenAPI
 * tinkoff.ru/invest OpenAPI.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: n.v.melnikov@tinkoff.ru
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "OrdersApi.h"
#include "Helpers.h"

#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

OrdersApi::OrdersApi(const QString &scheme, const QString &host, int port, const QString &basePath, const int timeOut)
    : _scheme(scheme),
      _host(host),
      _port(port),
      _basePath(basePath),
      _timeOut(timeOut),
      isResponseCompressionEnabled(false),
      isRequestCompressionEnabled(false) {}

OrdersApi::~OrdersApi() {
}

void OrdersApi::setScheme(const QString &scheme) {
    _scheme = scheme;
}

void OrdersApi::setHost(const QString &host) {
    _host = host;
}

void OrdersApi::setPort(int port) {
    _port = port;
}

void OrdersApi::setBasePath(const QString &basePath) {
    _basePath = basePath;
}

void OrdersApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void OrdersApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void OrdersApi::addHeaders(const QString &key, const QString &value) {
    defaultHeaders.insert(key, value);
}

void OrdersApi::enableRequestCompression() {
    isRequestCompressionEnabled = true;
}

void OrdersApi::enableResponseCompression() {
    isResponseCompressionEnabled = true;
}

void OrdersApi::abortRequests(){
    emit abortRequestsSignal();
}

void OrdersApi::ordersCancelPost(const QString &order_id, const QString &broker_account_id) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/orders/cancel");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("orderId")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(order_id)));

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("brokerAccountId")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(broker_account_id)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "POST");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &OrdersApi::ordersCancelPostCallback);
    connect(this, &OrdersApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void OrdersApi::ordersCancelPostCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    Empty output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit ordersCancelPostSignal(output);
        emit ordersCancelPostSignalFull(worker, output);
    } else {
        emit ordersCancelPostSignalE(output, error_type, error_str);
        emit ordersCancelPostSignalEFull(worker, error_type, error_str);
    }
}

void OrdersApi::ordersGet(const QString &broker_account_id) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/orders");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("brokerAccountId")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(broker_account_id)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &OrdersApi::ordersGetCallback);
    connect(this, &OrdersApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void OrdersApi::ordersGetCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    OrdersResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit ordersGetSignal(output);
        emit ordersGetSignalFull(worker, output);
    } else {
        emit ordersGetSignalE(output, error_type, error_str);
        emit ordersGetSignalEFull(worker, error_type, error_str);
    }
}

void OrdersApi::ordersLimitOrderPost(const QString &figi, const LimitOrderRequest &limit_order_request, const QString &broker_account_id) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/orders/limit-order");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("figi")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(figi)));

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("brokerAccountId")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(broker_account_id)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "POST");

    QString output = limit_order_request.asJson();
    input.request_body.append(output);

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &OrdersApi::ordersLimitOrderPostCallback);
    connect(this, &OrdersApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void OrdersApi::ordersLimitOrderPostCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    LimitOrderResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit ordersLimitOrderPostSignal(output);
        emit ordersLimitOrderPostSignalFull(worker, output);
    } else {
        emit ordersLimitOrderPostSignalE(output, error_type, error_str);
        emit ordersLimitOrderPostSignalEFull(worker, error_type, error_str);
    }
}

void OrdersApi::ordersMarketOrderPost(const QString &figi, const MarketOrderRequest &market_order_request, const QString &broker_account_id) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/orders/market-order");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("figi")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(figi)));

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("brokerAccountId")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(broker_account_id)));

    HttpRequestWorker *worker = new HttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    HttpRequestInput input(fullPath, "POST");

    QString output = market_order_request.asJson();
    input.request_body.append(output);

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &HttpRequestWorker::on_execution_finished, this, &OrdersApi::ordersMarketOrderPostCallback);
    connect(this, &OrdersApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void OrdersApi::ordersMarketOrderPostCallback(HttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    MarketOrderResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit ordersMarketOrderPostSignal(output);
        emit ordersMarketOrderPostSignalFull(worker, output);
    } else {
        emit ordersMarketOrderPostSignalE(output, error_type, error_str);
        emit ordersMarketOrderPostSignalEFull(worker, error_type, error_str);
    }
}

} // namespace OpenAPI
