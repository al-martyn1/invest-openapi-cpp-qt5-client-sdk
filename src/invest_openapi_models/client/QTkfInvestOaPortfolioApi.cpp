/**
 * OpenAPI
 * tinkoff.ru/invest OpenAPI.
 *
 * The version of the OpenAPI document: 1.0.0
 * Contact: n.v.melnikov@tinkoff.ru
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

#include "QTkfInvestOaPortfolioApi.h"
#include "QTkfInvestOaHelpers.h"

#include <QJsonArray>
#include <QJsonDocument>

namespace OpenAPI {

QTkfInvestOaPortfolioApi::QTkfInvestOaPortfolioApi(const QString &scheme, const QString &host, int port, const QString &basePath, const int timeOut)
    : _scheme(scheme),
      _host(host),
      _port(port),
      _basePath(basePath),
      _timeOut(timeOut),
      isResponseCompressionEnabled(false),
      isRequestCompressionEnabled(false) {}

QTkfInvestOaPortfolioApi::~QTkfInvestOaPortfolioApi() {
}

void QTkfInvestOaPortfolioApi::setScheme(const QString &scheme) {
    _scheme = scheme;
}

void QTkfInvestOaPortfolioApi::setHost(const QString &host) {
    _host = host;
}

void QTkfInvestOaPortfolioApi::setPort(int port) {
    _port = port;
}

void QTkfInvestOaPortfolioApi::setBasePath(const QString &basePath) {
    _basePath = basePath;
}

void QTkfInvestOaPortfolioApi::setTimeOut(const int timeOut) {
    _timeOut = timeOut;
}

void QTkfInvestOaPortfolioApi::setWorkingDirectory(const QString &path) {
    _workingDirectory = path;
}

void QTkfInvestOaPortfolioApi::addHeaders(const QString &key, const QString &value) {
    defaultHeaders.insert(key, value);
}

void QTkfInvestOaPortfolioApi::enableRequestCompression() {
    isRequestCompressionEnabled = true;
}

void QTkfInvestOaPortfolioApi::enableResponseCompression() {
    isResponseCompressionEnabled = true;
}

void QTkfInvestOaPortfolioApi::abortRequests(){
    emit abortRequestsSignal();
}

void QTkfInvestOaPortfolioApi::portfolioCurrenciesGet(const QString &broker_account_id) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/portfolio/currencies");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("brokerAccountId")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(broker_account_id)));

    QTkfInvestOaHttpRequestWorker *worker = new QTkfInvestOaHttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    QTkfInvestOaHttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &QTkfInvestOaHttpRequestWorker::on_execution_finished, this, &QTkfInvestOaPortfolioApi::portfolioCurrenciesGetCallback);
    connect(this, &QTkfInvestOaPortfolioApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void QTkfInvestOaPortfolioApi::portfolioCurrenciesGetCallback(QTkfInvestOaHttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    QTkfInvestOaPortfolioCurrenciesResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit portfolioCurrenciesGetSignal(output);
        emit portfolioCurrenciesGetSignalFull(worker, output);
    } else {
        emit portfolioCurrenciesGetSignalE(output, error_type, error_str);
        emit portfolioCurrenciesGetSignalEFull(worker, error_type, error_str);
    }
}

void QTkfInvestOaPortfolioApi::portfolioGet(const QString &broker_account_id) {
    QString fullPath = QString("%1://%2%3%4%5")
                           .arg(_scheme)
                           .arg(_host)
                           .arg(_port ? ":" + QString::number(_port) : "")
                           .arg(_basePath)
                           .arg("/portfolio");

    if (fullPath.indexOf("?") > 0)
        fullPath.append("&");
    else
        fullPath.append("?");
    fullPath.append(QUrl::toPercentEncoding("brokerAccountId")).append("=").append(QUrl::toPercentEncoding(::OpenAPI::toStringValue(broker_account_id)));

    QTkfInvestOaHttpRequestWorker *worker = new QTkfInvestOaHttpRequestWorker(this);
    worker->setTimeOut(_timeOut);
    worker->setWorkingDirectory(_workingDirectory);
    QTkfInvestOaHttpRequestInput input(fullPath, "GET");

    foreach (QString key, this->defaultHeaders.keys()) { input.headers.insert(key, this->defaultHeaders.value(key)); }

    connect(worker, &QTkfInvestOaHttpRequestWorker::on_execution_finished, this, &QTkfInvestOaPortfolioApi::portfolioGetCallback);
    connect(this, &QTkfInvestOaPortfolioApi::abortRequestsSignal, worker, &QObject::deleteLater); 
    worker->execute(&input);
}

void QTkfInvestOaPortfolioApi::portfolioGetCallback(QTkfInvestOaHttpRequestWorker *worker) {
    QString msg;
    QString error_str = worker->error_str;
    QNetworkReply::NetworkError error_type = worker->error_type;

    if (worker->error_type == QNetworkReply::NoError) {
        msg = QString("Success! %1 bytes").arg(worker->response.length());
    } else {
        msg = "Error: " + worker->error_str;
        error_str = QString("%1, %2").arg(worker->error_str).arg(QString(worker->response));
    }
    QTkfInvestOaPortfolioResponse output(QString(worker->response));
    worker->deleteLater();

    if (worker->error_type == QNetworkReply::NoError) {
        emit portfolioGetSignal(output);
        emit portfolioGetSignalFull(worker, output);
    } else {
        emit portfolioGetSignalE(output, error_type, error_str);
        emit portfolioGetSignalEFull(worker, error_type, error_str);
    }
}

} // namespace OpenAPI
